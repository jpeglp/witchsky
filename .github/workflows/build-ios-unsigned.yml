name: Build unsigned IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest # or macos-26-xlarge if Xcode 26 needed
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 5

      - uses: pnpm/action-setup@v6

      - name: 🔧 Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: 🪛 Setup jq
        uses: dcarbone/install-jq-action@v2

      - name: ⚙️ Install dependencies
        run: pnpm install --frozen-lockfile

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "latest-stable" # use whatever matches the repo's requirements

      - name: ☕️ Setup Cocoapods
        uses: maxim-lobanov/setup-cocoapods@v1
        with:
          version: 1.16.2

      - name: 🔤 Compile translations
        run: pnpm intl:build

      - name: ✏️ Write environment variables
        run: |
          # Minimal .env — omit secrets that aren't needed for a local build
          echo "EXPO_PUBLIC_RELEASE_VERSION=$(jq -r '.version' package.json)" >> .env
          echo "EXPO_PUBLIC_BUNDLE_IDENTIFIER=$(git rev-parse HEAD)" >> .env
          echo "EXPO_PUBLIC_BUNDLE_DATE=$(date -u +"%y%m%d%H")" >> .env

      # This generates the ios/ native project without building or signing
      - name: 🏗️ Prebuild (generate native iOS project via Expo)
        run: npx expo prebuild --platform ios

      - name: 🔧 Copy assets into generated iOS directory
        run: cp -R assets/. ios/Witchsky/

      - name: 📦 Install Pods (bundle + verbose)
        run: |
          set -o pipefail
          cd ios
          echo "=== Podfile location ==="
          pwd; ls -la
          echo "=== Podfile content (first 200 lines) ==="
          sed -n '1,200p' Podfile || true
          echo "=== Ruby & CocoaPods versions ==="
          ruby -v || true
          pod --version || true
          echo "=== Ensure node_modules exists (first 200 entries) ==="
          ls -la ../node_modules | sed -n '1,200p' || true
          echo "=== Using bundler to isolate cocoapods version ==="
          gem install bundler --no-document || true
          bundle init || true
          # ensure Gemfile contains cocoapods version pin
          grep -q "cocoapods" Gemfile || echo "gem 'cocoapods', '1.16.2'" >> Gemfile
          bundle install --jobs 4 || bundle install
          bundle exec pod install --repo-update --verbose

      - name: 🔍 Debug - list ios directory
        run: ls -la ios/

      - name: 🔨 Archive (no code signing)
        run: |
          set -o pipefail
          xcodebuild archive \
            -workspace ios/Witchsky.xcworkspace \
            -scheme Witchsky \
            -archivePath $RUNNER_TEMP/Witchsky.xcarchive \
            -sdk iphoneos \
            -arch arm64 \
            -configuration Release \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty

      # - name: 📦 Upload RUNNER_TEMP for inspection
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: runner-temp-debug
      #     path: ${{ runner.temp }}/

      - name: 🔍 Debug - list xcarchive contents
        run: find $RUNNER_TEMP/Witchsky.xcarchive -type d

      - name: 📦 Package unsigned IPA
        run: |
          mkdir -p $RUNNER_TEMP/Payload
          cp -r "$RUNNER_TEMP/Witchsky.xcarchive/Products/Applications/Witchsky.app" \
            $RUNNER_TEMP/Payload/
          cd $RUNNER_TEMP
          zip -r Witchsky.ipa Payload/

      - name: ⬆️ Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: Witchsky-ios.ipa
          path: ${{ runner.temp }}/Witchsky.ipa

      - name: ⬆️ Push File to R2 Bucket
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: r2 object put shit/Witchsky.ipa --file=${{ runner.temp }}/Witchsky.ipa
