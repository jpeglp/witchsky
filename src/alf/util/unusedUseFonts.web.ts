/*
 * Web fonts are loaded via `web/index.html` using stable asset paths.
 * Keeping this stub prevents expo-font's extraction helper from pulling
 * duplicate woff2 files into the JS bundle on web.
 */
export function DO_NOT_USE() {
  return [true] as const
}
